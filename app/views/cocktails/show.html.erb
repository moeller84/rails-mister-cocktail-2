<h1>Cocktails#show</h1>
<p>Find me in app/views/cocktails/show.html.erb</p>

<h3><%= @cocktail.name %></h3>

<%= link_to 'Add a dose', new_cocktail_dose_path(@cocktail) %>
<%= link_to "Delete a dose", cocktail_path(cocktail),
      method: :delete,
      data: { confirm: "Are you sure?" } %>

